数据格式: .csv .xml .json
https://github.com/eugenp/tutorials/tree/master/spring-batch

TODO. 运行前的Health Check检测Schema是否正确
https://docs.spring.io/spring-batch/reference/schema-appendix.html

TODO. 定义要执行的Job及Step步骤, 相同Job名称只会在BATCH_JOB_INSTANCE中记录一次
Job (name): 执行任务, 由JobLauncher启动执行
  -> JobRepository (dataSource)
    => JobExecutionListener (beforeJob, afterJob) 监听器/拦截器
    JobBuilder   SimpleJobBuilder   JobFlowBuilder
                  .start()           .start()
                  .next()            .flow()
                  .build()           .build()

   -> Step0 (name): Tasklet 任务步骤(准备工作)，可能不涉及数据操作
     => StepExecutionListener (beforeStep, afterStep)
      -> .tasklet(MyTasklet())

   TODO. 定义Step执行步骤: Reader -> Processor -> Writer
   -> Step1 (name): Chunk<I, O> 块执行步骤, 完整的读写数据流程
     => StepExecutionListener (beforeStep, afterStep)
       -> ItemReader
         => ItemReadListener (beforeRead, afterRead, onReadError)
         -> FieldSetMapper     原始文件中数据映射到读取对象
         -> FlatFileItemReader 从文件中读取数据
         -> JdbcCursorItemReader 从DB中获取数据

       -> ItemProcessor
         => ItemProcessListener (beforeProcess, afterProcess)
         -> ItemMapper      从读取对象映射到目标对象
         -> SourceExtractor 从原始对象中解析出适配内容

       -> ItemWriter
         => ItemWriteListener (beforeWrite, afterWrite, onWriteError)
         -> StaxEventItemWriter 写入XML文件中
         -> JdbcBatchItemWriter 存储到DB中